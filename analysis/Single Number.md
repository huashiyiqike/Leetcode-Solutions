<backquote><pre>
Given an array of integers, every element appears twice except for one. Find that single one.
</backquote></pre>

Two same Integer xor is 0.And O xor x is x.So all the Integer xor the result is the single one Integer.s