>Given a non-empty array of integers, return the k most frequent elements.

>For example,
>Given [1,1,1,2,2,3] and k = 2, return [1,2].

First use hash to record the times of appearance for each integer, then sort with heap. 
